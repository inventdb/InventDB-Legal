================================================================================
INVENTDB LEGAL ON EMERGENT — no GitHub
Order: paste PROMPT 1 -> do the UPLOAD yourself -> paste PROMPT 1.5 -> PROMPT 2.
Do not paste PROMPT 2 until `ls /app` shows backend/ frontend/ contract/.
================================================================================


-------------------------------------------------------------------------------
STEP 0 — make the archive (local PowerShell, from the project root)
-------------------------------------------------------------------------------
cd "c:\InventDB Legal"
$out = "$([Environment]::GetFolderPath('Desktop'))\inventdb-legal-source.zip"
git archive --format=zip -o $out HEAD -- . ':(exclude)CA_Litigation_Practice_Dataset.xlsx'
Get-Item $out | Select-Object FullName, Length

Expect ~0.8 MB / 280 files. If Length comes back 0 the command failed — delete the
file and run it again rather than uploading it.

`git archive` emits only tracked files, so node_modules, .venv, dist, .env and the
Playwright auth store cannot sneak in. The pathspec drops the 11 MB sample
workbook, which the application does not read at runtime.

COMMIT FIRST. `git archive HEAD` packages the last commit, not your working tree.
Run `git status` — if it is not clean, whatever is uncommitted will be missing
from the container.


-------------------------------------------------------------------------------
PROMPT 1 — first message in a new Emergent task
-------------------------------------------------------------------------------
Do not build an application. Do not write any application code yet.

I have a complete, finished codebase that I am going to upload into /app myself
through the VS Code editor. I need a running container and nothing else.

Specifically, do NOT scaffold: a FastAPI backend, a React or Next.js frontend, a
MongoDB database, authentication, or any starter files. Do not run
create-react-app. Do not add any dependency. Do not connect this to GitHub — I am
not using it.

Just initialise an empty workspace, confirm the container is up, tell me the
absolute path of the app directory and what (if anything) is already in it, and
then STOP and wait for my next message.


-------------------------------------------------------------------------------
UPLOAD — you, in the browser
-------------------------------------------------------------------------------
1. Click "Code" at the top right of the project. Copy the password, open the
   VS Code link in a new tab, paste the password.

2. Drag inventdb-legal-source.zip from your Desktop onto /app in the VS Code file
   explorer. Upload the zip, not 280 loose files. If VS Code opens it and says
   "file is not displayed... binary", ignore that and close the tab — it is just
   trying to preview the zip as text.

That is all you do by hand. PROMPT 1.5 makes the agent do the rest.


-------------------------------------------------------------------------------
PROMPT 1.5 — unpack the upload (paste this; no terminal needed)
-------------------------------------------------------------------------------
Before you paste: replace <slug> on the INVENTDB_BASE_URL line with your instance.

I have uploaded /app/inventdb-legal-source.zip. It is a COMPLETE, FINISHED
application — InventDB Legal. Unpack it for me. Do NOT build, configure, install
or run anything yet, and do not write any application code.

Do exactly this, in this order, and stop:

1. The backend/, frontend/, tests/, yarn.lock, README.md and .gitignore currently
   in /app are the starter files you generated. They are NOT my application and
   must not be merged with it. Move them aside, do not delete them:
       mkdir -p /app/_scaffold
       mv /app/backend /app/frontend /app/tests /app/yarn.lock /app/README.md /app/.gitignore /app/_scaffold/
   Leave /app/.emergent, /app/memory, /app/test_reports and /app/test_result.md
   exactly where they are — those are platform files, not starter code.

2. Extract the zip into /app, then delete the zip:
       cd /app && unzip -o inventdb-legal-source.zip && rm inventdb-legal-source.zip
   If unzip is not installed, use: python3 -m zipfile -e inventdb-legal-source.zip .

3. Create /app/backend/.env containing exactly this one line:
       INVENTDB_BASE_URL=https://<slug>.sandbox.inventdb.com

4. Commit the result as a baseline so I can see later what you changed:
       cd /app && git add -A && git commit -m "Import Legal source at 84b0efb"

5. Run `ls /app` and `ls /app/backend /app/frontend /app/contract` and paste the
   real output back to me.

Then STOP and wait. Do not install dependencies, do not touch supervisor, do not
start any service. I will send the next instruction.

Only when that output shows backend/ frontend/ contract/ do you paste PROMPT 2.


-------------------------------------------------------------------------------
PROMPT 2 — configure the platform (paste after the upload)
-------------------------------------------------------------------------------
I have uploaded a COMPLETE, WORKING application into /app myself — InventDB Legal,
a legal practice management system for a California litigation firm. It is
finished, tested and correct. Do NOT rebuild, rewrite, scaffold, modernise, or
create a second version of it. Read the code before you touch anything.

/app/_scaffold/ holds the starter files you generated earlier. It is dead weight I
kept only for reference. Do not read from it, merge it, or restore anything out of
it. The real application is /app/backend and /app/frontend.

READ THIS FIRST — three of your defaults are wrong here and will break it:
  - The backend is Flask, NOT FastAPI. Do not convert it.
  - The frontend is React 18 + Vite, NOT Create React App and NOT Next.js. There
    is no REACT_APP_BACKEND_URL. The Vite variable is VITE_API_BASE and it is
    already correct.
  - There is NO DATABASE. Not MongoDB, not Postgres, not SQLite. Do not add one,
    do not add an ORM, migrations, a users table or a session store, and do not
    set MONGO_URL. ALL data AND ALL authentication live in InventDB's REST API.
    The backend holds no credentials of its own — it forwards the caller's bearer
    token. If you find yourself reaching for a database you have misread this task.

Architecture you are working with:
- backend/   Python 3.11+, Flask + requests. Pure-Python, no compiled deps.
             Entrypoint backend/wsgi.py exposes `app`. Deps backend/requirements.txt.
             Every route is already prefixed /api — it matches the ingress as-is.
- frontend/  React 18 + TypeScript + Vite. `npm run build` outputs frontend/dist.
             The SPA calls a relative "/api" (VITE_API_BASE defaults to /api), so
             it works on whatever origin serves it. Leave that default alone.

Your only job is to make it run here, as TWO services matching the standard
ingress (/api -> 8001, everything else -> 3000):

1. Backend service, bound exactly to 0.0.0.0:8001, started from backend/:
      pip install -r backend/requirements.txt
      gunicorn -w 2 --worker-class gthread --threads 8 --timeout 0 \
               -b 0.0.0.0:8001 wsgi:app
   --timeout 0 and threaded workers are REQUIRED. /api/analyze/chat/stream and
   /api/reports/templates/<id>/edit/stream are long-lived Server-Sent Events
   relays that can idle for minutes between chunks while the agent reasons.
   Default sync workers would kill them mid-stream.

2. Frontend service on port 3000, serving the built SPA:
      cd frontend && npm install && npm run build
      npx vite preview --host 0.0.0.0 --port 3000
   (Any static server with SPA history fallback is fine. It must serve
   frontend/dist and fall back to index.html for client-side routes.)

3. Repoint the existing supervisor `backend` and `frontend` programs at those two
   commands, so `sudo supervisorctl restart all` brings this app up. Do not leave
   uvicorn or a CRA dev server running.

4. Configuration is already in backend/.env. Read it, do not overwrite it, and do
   not invent new variables.

Note on ports: backend/app/config.py defaults API_PORT to 8010 and the Vite dev
server to 5174. Those defaults keep the app off the ports a sibling InventDB app
uses locally; they do not apply here, because gunicorn and vite are both given
their port explicitly. Do not "correct" the defaults, and leave
`_refuse_occupied_port` in backend/app/main.py alone — it guards the dev
entrypoint only and never runs under gunicorn.

Verify before reporting done, and paste the ACTUAL output:
      curl -s localhost:8001/api/health   -> {"ok": true, ... "namespace": "legal"}
      curl -s localhost:3000/             -> the SPA html
   and the app must load at / and show the login page.

Constraints:
- Do NOT modify anything under backend/app/, frontend/src/, backend/tests/,
  frontend/e2e/ or contract/. If something fails, fix the environment
  configuration, not the application code.
- Keep the backend pure-Python. Do not add FastAPI, pydantic, or anything needing
  a C or Rust toolchain.
- Do not install Playwright browsers unless I ask.
- Do not "fix" any of the load-bearing oddities listed at the end of this file.

Report exactly which files you changed.


-------------------------------------------------------------------------------
AFTER — check for drift, then prove parity (you, in the VS Code terminal)
-------------------------------------------------------------------------------
cd /app && git add -A && git commit -m "Emergent configuration" && git show --stat HEAD

Anything under backend/app/, frontend/src/ or contract/ is drift, not a fix:
     git checkout HEAD~1 -- backend/app frontend/src contract

Gates — these are the numbers this build has to reproduce:
     pip install -r backend/requirements-dev.txt
     cd /app/backend && python -m pytest         -> 1176 passed, 6 xfailed
     cd /app/frontend && npm run typecheck       -> 0 errors
     cd /app/frontend && npm run typecheck:e2e   -> 0 errors
     npx playwright install chromium
     npx playwright test --workers=2             -> 560 passed across 25 spec files
     curl -s localhost:8001/api/health           -> "ok": true, "namespace": "legal"
     curl -s localhost:3000/                     -> SPA html
     grep -rEi 'psycopg|sqlalchemy|sqlite|pymongo|motor|mongo' backend/ frontend/src/
                                                 -> no hits

Note: "frontend_bundled": true in /api/health is NOT proof the SPA is being served
here. Flask still finds ../frontend/dist, but the page comes from the frontend
service on 3000. Check port 3000 separately.

If Chromium will not launch, it is missing system libraries — ask the agent to
install them. Bound the workers (--workers=2); at default parallelism the run
exhausts container memory and gets killed mid-suite, which looks like a test
failure and is not.

Once the app runs green, delete the leftovers:  rm -rf /app/_scaffold


================================================================================
LOAD-BEARING ODDITIES — do not "clean these up"
Every one of these was found the hard way. They look wrong and are not.
================================================================================

1. Flask must NOT sort JSON keys (app.json.sort_keys = False). Row key order IS
   column order through a thin proxy; sorting them reorders every table.

2. The SSE endpoints set X-Accel-Buffering: no and must never be buffered, parsed,
   reformatted or accumulated. Relay them untouched.

3. File download / preview / thumbnail STREAM bytes. Never buffer a file into
   memory.

4. Select-option colours are set on the <option> elements, not the parent control.
   The OS draws select menus and ignores the parent's styles.

5. InventDB's SQL engine collapses a GROUP BY to a single row when the WHERE
   clause contains a function call. backend/app/routers/dashboard.py and
   reports.py therefore GROUP BY a column instead of filtering on it — e.g. the
   cash-flow split groups on `deposited_to` rather than using
   `WHERE lower(deposited_to) = '...'`. Rewriting those into the "obvious" filtered
   form silently returns one row holding the grand total. Tests pin this.

6. InventDB caps a statement at 1000 rows. Every dashboard figure is therefore a
   SUM/COUNT/GROUP BY executed inside InventDB, and free-text search is an OR of
   LIKEs run upstream — not a Python rollup over a page of results. With 34,000
   time entries, a Python sum over `SELECT *` would report the total of the first
   page. Do not "simplify" these into client-side aggregation.

7. frontend/src/pages/importRun.ts holds the running import in a module-level
   store, deliberately OUTSIDE React. Moving it into component state re-introduces
   a data-loss bug: navigating away unmounts the page, aborts the batches
   mid-flight, and leaves half the rows written with nothing on screen to say so.
   frontend/e2e/import-navigation.spec.ts guards this.

8. Reports templates and Analyze threads live in InventDB's _System namespace, at
   the instance level — they are shared with SOAR and with any sibling app on the
   same instance. That is upstream behaviour, not a bug in this app.
